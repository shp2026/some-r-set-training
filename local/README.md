# Local Training

Steps: 
 1. If you haven't already, install UV by following the [instructions](https://docs.astral.sh/uv/getting-started/installation/).  
 1. Open a terminal, navigate to the `local` directory. 
 1. Run `uv sync`
 1. Pick a file and run it using UV, e.g. `uv run somerset_deck.py`