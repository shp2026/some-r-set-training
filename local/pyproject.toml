[project]
name = "some-r-set_local_training"
version = "0.1.0"
description = "Trains a model to play the Some-R-Set card game using local hardware."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gymnasium>=1.3.0",
    "numpy>=2.4.6",
]
